// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.klever.app"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
